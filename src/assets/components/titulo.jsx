import React from 'react';

function  Title(props) {
    // recuerda que este componente es funcional
    // se deben incluir estilos para hacerlo mas atractivo
    return <h1>{props.title}</h1>;



}
export default Title;